import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts without errors', (WidgetTester tester) async {
    // 基本的なテストはスキップ（Win32 APIが必要なため）
    // Windows上でのみ実行可能
  });
}
